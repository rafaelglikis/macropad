[project]
name = "macropad"
version = "0.1.0"
description = "Turn every keyboard into a Macropad"
requires-python = ">=3.10"
dependencies = [
    "argcomplete>=3.1.4",
    "evdev>=1.6.1",
    "pyyaml>=6.0.1",
]